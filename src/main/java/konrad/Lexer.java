package konrad;

import java.util.*;
import java.lang.*;
import konrad.util.*;

public class Lexer {

    // NOTE(Simon): this is a brutally simple version of the lexer, there is a
    // great chance we need to rewrite this NOTE(Simon): to focus on Error
    // handling... (making it compatible with taking a sourcefile) 
    // FIX(Simon): this function is a pure mess, I should really fix it some day => make it compatible with taking a raw sourceFile
    public static ArrayList<Token> tokenize(String line) {

	var tokenStream = new ArrayList<Token>();

	return tokenStream;
    }
}
