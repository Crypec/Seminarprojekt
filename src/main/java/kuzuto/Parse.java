package kuzuto;

import kuzuto.util.*;

public class Parse {
    // /*
    //   Parse function call:

    //   foo(3 + 3)
    //   a := foo(3 + 3, "bar") - 1
    // */
    // public static Expr functionCall(Iter<Token> it, Token functionName) {

    // 	Token functionName = it.next();
    // 	it.next();
    // 	while (it.hasNext()) {
	    
    // 	}
    // 	return new Expr.Call(1);
    // }

    // public static Expr expression(Iter<Token> it) {
    // 	return new Expr.Literal(true);
    // }
}
