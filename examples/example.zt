Typ: Person {
	 name: Text,
	 alter: Zahl,
}
impl Person {
	 fn neu(name: Text, alter: Zahl) -> Person {
	      rueckgabe Person {
		  			name: name,
					alter: alter,
		  };
	 }
	 fn darf_fahren(selbst) -> Bool {
	 	 rueckgabe selbst.alter >= 18;
	 }

}

fn start() {
     simon := Person::neu("simon", 18);
     a := 10;
}